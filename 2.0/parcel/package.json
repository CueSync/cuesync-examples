{
  "name": "CueSync with parcel",
  "description": "Include CueSync's JavaScript with Parcel.",
  "version": "0.0.0",
  "private": true,
  "repository": "cuesync/cuesync-examples",
  "license": "MIT",
  "scripts": {
    "start": "parcel serve src/index.html assets/transcripts/natgeo.vtt assets/transcripts/natgeo_hindi.vtt --public-url / --dist-dir dist",
    "build": "parcel build src/index.html assets/transcripts/natgeo.vtt assets/transcripts/natgeo_hindi.vtt --public-url / --dist-dir dist",
    "test": "npm run build"
  },
  "dependencies": {
    "@cuesync/cuesync": "^2.0.0-alpha3"
  },
  "devDependencies": {
    "@parcel/transformer-raw": "^2.12.0",
    "@parcel/transformer-sass": "^2.14.4",
    "parcel": "^2.14.4"
  }
}
